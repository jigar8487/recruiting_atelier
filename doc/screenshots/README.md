# Screenshots

Drop your captures here, named exactly:

- `home.png` — `/` (home, magazine cover)
- `run-timeline.png` — `/run` with a live SSE timeline mid-stream
- `tools.png` — `/tools` (anatomy + pattern + live registry)
- `kb-editor.png` — `/kb` with the right-side TipTap editor open

Recommended: 1600×1000 PNG, browser chrome cropped out.
